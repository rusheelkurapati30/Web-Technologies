var btn = document.createElement("button");
var symbol;
var button;
function generateButton()
{
	for(symbol = 0; symbol < 10; symbol++)
	{
		document.getElement("button").innerHTML = symbol;
	}
}